#include<bits/stdc++.h>
using namespace std;

// reference using pointer:
// void increment(int *a){
//     (*a)++;
// }

int main(){
    /*
    // cout add:use (&)
    int x =10;
    int y =x;
    cout<<&x<<" "<<&y;

    // store address and cout value of var: use *(read itvalue at) 
   int a=7;
   int *point;
   point = &a;
   cout<<*point;

    // change value:
   int a=7;
   int *point;
   point = &a;
   cout<<*point<<endl;
   *point = 9;
   cout<<*point;

    //for array
        // it store continuous addr and name of array is pointer itself.
   int a[10];
   cout<<a<<endl;
   cout<<*a<<endl;
   cout<<&a;

    //double pointer (**) : used to store value of pointer
    int x=4;
    int *p;
    p=&x;
    cout<<"add of x is "<<p<<" value of x using *p:"<<*p<<endl;
    int **pointer_point;
    pointer_point=&p;
    cout<<"add of p is "<<pointer_point<<" value of x using **pointer_point:"<<**pointer_point<<endl;

    reference using pointer:
   int x=7;
   cout<<"bfore :"<<x<<endl;
   increment(&x);
    cout<<"after :"<<x;

    */

    }