#include<bits/stdc++.h>
using namespace std;

// global array
// const int n = 2e7;
// int a[n];


int main(){
    /*
    // Array:to store collection of datatype
    // syntax:int a[size of array] , assign value: a[5] =15; note:-index<array size
 // que:sum of array value
   int n;
   cin>>n;
   int a[n];
   for(int i=0;i<n;i++){
    cin>>a[i];
   }
   int sum = 0;
   for(int i=0;i<n;i++){
    sum = sum + a[i];
   }
   cout<<sum<<endl;
    
    // matrix:
//  que:matrix of n*m
   int n;
   int m;
   cin>>n>>m;
   int a[n][m];
   for(int i=0;i<n;i++){
    for(int j=0;j<m;j++){
        cin>>a[i][j];
    }
   }
   for(int i=0;i<n;i++){
    for(int j=0;j<n;j++){
        cout<<a[i][j]<<" ";
    }
    cout<<endl;
   }

//size of array is arround order : 10^5 (local) and 10^7 (global)
   int n = 2e5;
   int a[n];
   a[n-1]=7;
   cout<<a[n-1];
    */

}