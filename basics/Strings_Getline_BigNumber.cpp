#include<bits/stdc++.h>
using namespace std;

int main(){
    /*
    // Strings:written in double quotes, Assign it using string ,take word until whitespace come out
    // concat string using +
   string name = "Sahil";
   string surname = "Dantani";
   string fullname = name + surname;
   cout<<fullname<<endl;

    //access character using index : name[1]
    // we can also assign value using index : name[0] = 'R' ,here we use single quote because name[0] is char;
    // size or length of string {.size() or .length()}
    // multi input: string str1,str2
     
    // getline : used to take hall sentence:
    // syntax:getline(cin,string_name);
    // when you take multi input using getline you have to use cin.ignore() after taking cases number;
    int t;
    cin>>t;
    cin.ignore();
    while(t--){
    string a ;
    getline(cin,a);
    cout<<a<<endl;
    }

    // .push_back():add char at the last of string
    // que:reverse string
   string str;
   cin>>str;
   string str_rev;
   for(int i = str.length()-1;i>=0;i--){
    str_rev.push_back(str[i]);
   }
   cout<<str_rev<<endl;

//que>if you have very large number you have to solve it using string
   string num;
   cin>>num;
   int last_digit=num[num.length()-1] - '0';
   cout<<last_digit<<endl;

*/

}